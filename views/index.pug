extends layout

block content
  h1= title
  p Welcome to Catbree's Paint House, a very basic paint manufactory inventory website built with Express (Node).

  p The shop has the following counts: 

  ul 
    li #[strong Categories:] #{category_count}
    li #[strong Items:] #{item_count}