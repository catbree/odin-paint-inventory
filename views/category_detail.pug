extends layout 

block content 
    h1= category.name

    if (category_items.length > 0)
        ul 
            each item in category_items 
                li 
                    a(href=item.url) #{item.name}
                    |  ($#{item.price}.00, #{item.status})
    
    else 
        p There are no items in this category.